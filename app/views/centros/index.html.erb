<% @card_title = "Relação de centro" %>
<% @btn_new = btn_new(new_centro_path) %>

<div class="table-responsive">
  <%= table_for(@centros, 'index') do %>
    <% field_table('Nome') { |f| f.nome} %>
    <% field_table('Situação') { |f| situacao(f.ativo)} %>
    <% btn_for %>

  <% end %>
</div>