<%= simple_form_for(@fornecedor) do |f| %>
	<div class="form-row justify-content-center">
		<div class="col-sm-12 col-md-10 col-lg-8">
			<div class="form_group">
				<div class="form-row">
					<div class="form-group col-md-4">
						<%= f.input :documento, placeholder:"Digite o documento" %>
					</div>
					<div class="form-group col-md-8">
						<%= f.input :nome, placeholder:"Digite o nome" %>
					</div>
				</div>
				<%= f.input :endereco, placeholder:"Digite o endereco", label: 'Endereço' %>
				<div class="form-row">
					<div class="form-group col-md-7">
						<%= f.input :cidade, placeholder:"Digite o cidade" %>
					</div>
					<div class="form-group col-md-5">
						<%= f.input :telefone, placeholder:"xx-x xxxx-xxxx" %>
					</div>
				</div>

				<%= f.input :ativo, placeholder:"Digite o ativo" %>
				<hr>
				<%= btn_save %>
				<%= btn_cancel(fornecedors_path) %>
			</div>
		</div>
	</div>
<% end %>
