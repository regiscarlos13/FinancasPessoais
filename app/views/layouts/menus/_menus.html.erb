<nav class="navbar sticky-top  navbar-expand-lg navbar-dark bg-primary">
  <a class="navbar-brand" href="/">SFIP</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navsite">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navsite">
    <ul class="navbar-nav">
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" data-toggle="dropdown">Cadastros</a>
        <div class="dropdown-menu">
          <%= render partial: "layouts/menus/cadastros" %>
        </div>
      </li>
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" data-toggle="dropdown">Lançamentos</a>
        <div class="dropdown-menu">
          <%= render partial: "layouts/menus/lancamentos" %>
        </div>
      </li>
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" data-toggle="dropdown">
        Relatórios</a>
        <div class="dropdown-menu">
          <%= render partial: "layouts/menus/relatorios" %>
        </div>
      </li>
    </ul>
    <ul class="navbar-nav ml-auto" >
     <li class="nav-item">
      <button type="button" class="btn btn-outline-light" data-toggle="modal" data-target="#ano_modal">
        
        <%= @select_btn_ano %>
    
     </button>


   </li>
   <li class="nav-item dropdown">
    
    <%= link_to '#', class: 'nav-link dropdown-toggle', id: 'navbarDropdown', data: { toggle: 'dropdown' } do %>
        <i class="far fa-user-circle"> </i> <%= current_user.email %>
    <% end %>

      <div class="dropdown-menu">

       <%= render partial: 'layouts/menus/email' %>
     </div>
   </li>
 </ul>
</div>
</nav>
<%= render partial: 'layouts/menus/modal_menus' %>